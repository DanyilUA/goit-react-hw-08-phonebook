import { RegisterForm } from "components/RegisterForm/Register";

const RegisterPage = () => {
    return (
        <div>
            <RegisterForm />
        </div>
    );
};

export default RegisterPage;
